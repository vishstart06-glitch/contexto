name: Test Workflow

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main, vishstart06-glitch-patch-1]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Run tests
      run: |
        echo "Running login authentication tests..."
        
        # Simulate a failing test (for demo purposes)
        echo "Testing login function..."
        echo "E
